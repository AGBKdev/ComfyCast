[project]
name = "comfycast"
description = "Run-on-Box button for ComfyUI: execute the workflow you're editing on a remote GPU machine, with live progress, compressed previews, and on-demand full-res fetch."
version = "1.4.1"
requires-python = ">=3.9"
license = { file = "LICENSE" }
dependencies = []

[project.urls]
Repository = "https://github.com/AGBKdev/ComfyCast"

[tool.comfy]
PublisherId = "agbk"
DisplayName = "ComfyCast"
Icon = ""
